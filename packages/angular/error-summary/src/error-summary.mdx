import {
  Meta,
  Primary,
  Canvas,
  Controls,
  Story,
} from '@storybook/addon-docs/blocks'
import { OfficialDocsLink } from '../../.storybook/components/OfficialDocsLink'

import * as ErrorSummaryStories from './error-summary.stories'

<Meta of={ErrorSummaryStories} />

# ErrorSummary

<OfficialDocsLink href="https://designsystemet.no/no/components/docs/error-summary/overview" />

`ErrorSummary` er en oppsummering av feil. Den gir brukeren oversikt over feil eller mangler som må rettes på en side eller trinn, for å komme videre.

<Primary />
<Controls />

## Bruk

```ts
import { ErrorSummary } from '@ks-digital/designsystem-angular/error-summary'

@Component({
  imports: [ErrorSummary],
  template: `
    <div ksd-error-summary>
      <h2 ksd-heading>For å gå videre må du rette opp følgende feil:</h2>
      <ul class="ds-list">
        <li>
          <a ksd-link data-color="neutral" href="#field-id">
            Feilmelding som matcher feltets feilmelding
          </a>
        </li>
      </ul>
    </div>
  `,
})
export class App {}
```

## Kodeeksempler

### Med skjema

Se "WithForm" story for et komplett eksempel med skjemafelt og ErrorSummary.

<Canvas of={ErrorSummaryStories.WithForm} />
